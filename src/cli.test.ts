/**
 * Placeholder test suite for task 1.1 scaffold.
 * Real tests for CLI arg parsing and module contracts are added in tasks 1.2+.
 */
import { describe, it, expect } from "vitest";

describe("harness scaffold (1.1)", () => {
  it("placeholder: test framework is wired up", () => {
    expect(true).toBe(true);
  });
});
